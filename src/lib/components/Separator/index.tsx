import styled from 'styled-components';
import { theme } from '../../styles/theme';

export const Separator = styled.hr`
  width: 100%;
  height: 2px;
  background-color: ${theme.colors.secondaryA};
`;
